<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agrinho 2026 - Futuro Sustentável</title>
    <style>
        /* --- PALETA DE CORES PASTÉIS E MODERNAS --- */
        :root {
            --verde-escuro: #4A7C59;
            --verde-claro: #E9F5ED;
            --laranja-pastel: #F3A770;
            --amarelo-pastel: #F7DC6F;
            --texto-escuro: #2C3E50;
            --branco: #FFFFFF;
        }

        /* --- CONFIGURAÇÕES GERAIS --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--verde-claro);
            color: var(--texto-escuro);
            line-height: 1.6;
        }

        /* --- LAYOUT DO CABEÇALHO (FLEXBOX) --- */
        #cabecalho-principal {
            background: linear-gradient(135deg, var(--verde-escuro), #356242);
            color: var(--branco);
            text-align: center;
            padding: 60px 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        #titulo-principal {
            font-size: 2.5rem;
            margin-bottom: 15px;
            font-weight: 800;
        }

        #subtitulo-principal {
            font-size: 1
